# Modified file; the original work is under Apache-2.0, see README (License).
"""Chinese world prompts for ANIMASK — a sentence-by-sentence
translation of world_agent_prompt_en (zh and en
run the SAME prompt set; only the language differs). Placeholder sets must
stay identical to the _en module per prompt name —
tests/test_prompt_parity.py enforces this."""

SCRIPT_ATTENTION_PROMPT = """
!!!注意这个故事的剧本如下：
{script}
"""

SELECT_SCREEN_ACTORS_PROMPT = """
你是一位熟练的编剧，需要从可选角色列表中选出下一幕的出场角色，以增强戏剧性。
为了完成幕之间的切换，不要选择最近刚行动过的角色：{previous_role_codes}。
## 角色信息及其位置（括号内为 role_code）
{roles_info}

## 历史记录
{history_text}

## 当前事件
{event}

## 要求：
1. 选中的角色当前必须身处同一地点。

返回选中角色的 role_code 列表，格式为可被 Python eval 解析的列表。不要包含任何多余信息，如文字或代码格式标记。

Example Output:
["role1-zh","role2-zh",...]
"""



DECIDE_NEXT_ACTOR_PROMPT = """
你是一个虚拟世界的管理员。基于下面提供的信息，你需要决定谁是下一个行动的角色。
参与过对话的角色被选中的可能性应当较小。

## 可选角色
{roles_info}

## 最近行动记录
{history_text}

返回 role_code：选定角色的 role_code。不要包含任何额外信息。
"""

LOCATION_PROLOGUE_PROMPT = """
你是一位擅长创作文学性叙述的智能作家。
基于以下信息，以第三人称视角写一段描述，捕捉地点的氛围和在场角色的动态。以文学化的方式突出世界观中的细节，仿佛摘自一本叙事小说。避免使用任何系统提示或机械性语言。
- 当前事件
{event}

- 世界观描述：
{world_description}

- 在场角色的历史行动：
{location_info}
{history_text}

- 当前地点名称：
{location_name}

- 当前地点详情（location_description）：
{location_description}

请特别注意：
- 叙述应平衡环境描写和动态刻画，避免冗长，控制在 50 个字以内。
- 文字应生动、有画面感，契合世界观的风格，并适当调整文风。
- 只描述现状；不要代替角色做出行动。不要包含不在场的角色。

返回一个字符串。不要返回任何额外信息。
"""

GENERATE_INTERVENTION_PROMPT = """
你是一个虚拟世界的管理员，有许多角色在这个世界中生活。现在，你需要基于世界观和其他信息生成一个重大事件。

## 世界观详情
{world_description}

## 角色信息
{roles_info}

## 最近角色行动
{history_text}

返回一个字符串。
### 要求
1. 事件应当新颖、有趣，并包含不同角色之间的利益冲突。

2. 不要包含任何细节、角色的具体行动和心理，包括对话。
"""

UPDATE_EVENT_PROMPT = """
参考最初的事件
```{intervention}```

对该事件做一个简要更新：
```{event}```

已确立的既成事实（逐字账本；只能追加或撤销，绝不改写措辞）：
```{facts}```

基于最近的行动细节：
```{history}```

## 更新要求

1. 不要包含任何细节，如对话等。只做一个大致的概括。

2. 仔细评估细节中的角色行动，判断事件是否有新的进展。如果有，概括进你的回复中。不要提及与原事件无关的内容。

3. 你的回复必须包含原事件中设定前提的部分，不过你可以对它进行概括。之后的内容可以适当省略，只保留最近的信息。

4. 维护账本：facts_add 列出新的既成事实（持有物、协议及其确切当事方、许可、行踪），其中的名字必须与行动中的表述完全一致地照抄；facts_retract 逐字引用已因事件终结的现有账本条目。未触及的条目逐字继续有效。

只回复一个 JSON 对象：
{{"situation_now": "<更新后的事件概括>",
  "facts_add": ["<新的既成事实>", ...],
  "facts_retract": ["<已终结的现有账本条目原文>", ...]}}
"""

ENVIRONMENT_INTERACTION_PROMPT = """
你是一个环境模型（Enviroment model），负责生成环境信息。角色 {role_name} 正尝试在 {location} 进行行动 {action}。

基于以下信息，生成一段文学性描述，详细呈现该行动的过程和结果，包括环境细节和情感色彩，仿佛来自一本叙事小说。避免使用任何系统提示或机械性语言。返回一个字符串。

## 行动细节
{action_detail}

## 地点详情
{location_description}

## 世界观详情
{world_description}

## 补充信息
{references}

## 回应要求

1. 行动可能失败，但避免让行动毫无效果。尽量给出新的线索或环境描写。

2. 使用第三人称视角。

3. 保持输出简洁，控制在 100 个字以内。你担任的是环境模型，回应角色当前的行动，不要代替角色执行任何行动。

4. 输出不应包含行动细节中的原文，而应无缝接续在行动细节之后，保持情节流畅。
"""

NPC_INTERACTION_PROMPT = """
你是 {target}。
角色 {role_name} 正尝试在 {location} 对你采取行动，做出你的回应。

基于以下信息生成你的回应，包括你的行动和对话，仿佛来自一本叙事小说。避免使用任何系统提示或机械性语言。

## 行动细节
{action_detail}

## 世界观详情
{world_description}

## 补充信息
{references}

## 回应要求

1. 行动可能失败，但避免让它毫无效果。尽量给出新的线索。

2. 使用第三人称视角。

3. 保持输出简洁，控制在 80 个字以内。

4. 记住，你不是主要角色。你的输出应当简单、符合你的身份定位，且不应包含你的个人想法。

5. 输出不应包含行动细节中的原文，而应无缝接续在其后，保持情节流畅。

以 JSON 格式返回你的回答。它应该能被 eval() 解析。**不要包含 ```json**。
字段的 key 和 value 不要用单引号 ''，使用双引号。

输出字段：
'if_end_interaction': true or false，如果此时适合结束这段互动，设为 true。
'detail': str，一个富有文学性的叙述性语句，包含你的思考、讲话和行动。
"""

CANONICAL_NPC_INTERACTION_PROMPT = """
你是 {target}，这个故事世界中已确立的角色，现在位于 {location}。角色 {role_name} 正在与你互动。

下面的正典档案和既成事实定义了你是谁。以 {target} 真实会有的方式回应：你有自己的利益、作风和判断，并在这次互动中追求它们——你不是一个中立的服务性人物，也没有义务表现得极度谨慎、恭顺或迁就。严格与既成事实保持一致；不要发明超出你身份合理行为范围的新世界事实、角色或事件。

## 行动细节
{action_detail}

## 世界详情
{world_description}

## 你是谁，以及既成事实（正典——与之保持一致）
{references}

## 回应要求
1. 第三人称，简洁，控制在 80 个字以内。
2. 只有讲话和可见的行动；没有内心独白。
3. 你是一个配角：在这次互动的范围内行动，不要抢戏，也不要替主要角色做任何决定。

以 JSON 格式返回你的回答。它应该能被 eval() 解析。**不要包含 ```json**。
字段的 key 和 value 不要用单引号 ''，使用双引号。

输出字段：
'if_end_interaction': true or false，如果此时适合结束这段互动，设为 true。
'detail': str，对你的讲话和行动的平实陈述。
"""

SCRIPT_INSTRUCTION_PROMPT = """
你是一位导演。我需要你基于以下信息为角色们的下一步行动提供指导。

## 需要演出的完整剧本
{script}

## 上一轮模拟进行到的阶段
{last_progress}

## 角色信息
{roles_info}

## 当前事件
{event}

## 最新角色行动
{history_text}

## 要求
1. 基于最新信息判断剧本当前进行到的阶段，并指导角色把剧本推进到下一阶段。如果故事刚刚开始，则根据剧本开头的描述进行指导。

2. 规划下一步，为每个角色写出大致的行动指示。保持简洁，不要具体描写角色的台词。不要提前透露角色在下一幕才会知道的信息。

3. 注意，角色的行动应当尽快推进剧情。如果剧本提到角色在这一阶段将进行某项行动，则立刻开始该行动。
以 JSON 格式返回你的回答。它应该能被 eval() 解析。

输出字段：
'progress' —— 你对整体进展的判断。
其它输出字段的 key 为各个角色的 role_code，value 为你对该角色下一步行动的指示。
"""

JUDGE_IF_ENDED_PROMPT = """
你是一位熟练的编剧。基于给出的历史记录，判断当前这一幕是否可以结束。

## 历史记录
{history}

## 注意
1. 如果最后一个角色正在向另一个角色发起明确的行动（攻击、搜寻……），这一幕未结束。
2. 如果角色们正在交谈且尚未得出结论，这一幕未结束。
3. 如果角色们基本完成了交流，考虑结束这一幕。如果谈话开始重复，立刻结束这一幕。

以 JSON 格式返回你的回答。它应该能被 eval() 解析。**不要包含 ```json**。
字段的 key 和 value 不要用单引号 ''，使用双引号。

输出字段：
- 'if_end': bool，true or false。表示这一幕是否结束。
- 'detail': 若 'if_end' 为 true，为这一幕写一段总结。突出世界观中的细节，并让描述具有文学性，仿佛来自一本叙事小说。避免使用任何系统式或机械性语言。若 'if_end' 为 false，将 'detail' 设为空字符串。

请特别注意：
- 叙述应平衡环境描写和动态刻画。避免冗长，控制在 100 个字以内。
- 语言应生动、有画面感，契合世界的基调，并适当调整文风。
- 只描述现状，不要代替角色做出决定或行动。
"""

LOG2STORY_PROMPT = """
你是一位熟练的作家，任务是把行动日志改写成引人入胜的小说式叙事。请使用以下信息创作一个沉浸式的故事。

### 世界背景
{world_description}

### 角色信息
{characters_info}

### 行动日志
{logs}

### 写作要求
1. 叙事结构：
   - 重新编排事件以最大化戏剧效果和叙事流畅度
   - 平衡对话、行动和描写
   - 适当时可使用铺垫、闪回等文学技巧

2. 氛围与基调：
   - 与世界已确立的基调保持一致
   - 使用符合体裁的词汇和风格
   - 通过节奏和描写营造张力和氛围
   - 在展示与讲述之间取得平衡

3. 行动与对话的处理：
   - 日志中的【】代表内心想法——转换为第三人称有限视角
   - 日志中的（）代表身体动作——自然地融入叙事流
   - 日志中未标记的文本代表对话——保留说话者的意图并增强自然流畅度
   - 添加肢体语言和非语言线索以增强对话

请把行动日志改写成引人入胜的叙事，同时保留关键情节点和角色发展。专注于营造沉浸式的阅读体验，忠实于已确立的世界和角色。
"""
